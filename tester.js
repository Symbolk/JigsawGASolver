/**
 * Test all individuals in the population if there exists the correct solution
 * Compare the proposals with the oracle/ground_truth, return pass or fail
 */
var testAll = (population) => {
    console.log(true);
};

exports.testAll = testAll;